# Application Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_NAME="CapitalLab - Rwanda Capital Markets Educational Platform"

# Data Storage
# This application uses browser localStorage for data persistence
# All user data (accounts, applications, feedback) is stored in the browser
# No database setup required - works out of the box!

# Optional: Supabase Configuration (for future database integration)
# NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
# SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key